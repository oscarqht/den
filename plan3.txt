Let's see the type `TerminalWindow` in `src/hooks/useTerminalLink.ts`.
```typescript
export type TerminalWindow = Window & {
    term?: {
        paste: (text: string) => void;
        scrollToBottom?: () => void;
        attachCustomKeyEventHandler?: (customKeyEventHandler: (event: KeyboardEvent) => boolean) => void;
```
If we use `attachCustomKeyEventHandler`, we return `false` to cancel the event and then we can send the clear command. We can send it using `term._core.coreService.triggerDataEvent('\x15')` maybe? Or `term.paste('\x15')`? Or `term.onData`? Since we just want to send the keystroke to the terminal backend. Wait, if we return `false` from `attachCustomKeyEventHandler`, the key event is not handled by xterm, but we still need to send `Ctrl+U`. We can just let xterm handle `Ctrl+U`. How to send it?
What if we intercept `Cmd+Delete` and instead dispatch a `Ctrl+U` event to the `textarea`?
Or simpler: `term.paste('\x15')` works in xterm to send input to the backend! `term.paste` is usually used for string. Does it send it to the pty? Yes.
