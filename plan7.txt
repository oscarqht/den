Let's check `src/hooks/useTerminalLink.ts` to see what types are available for `TerminalWindow['term']`:

```typescript
export type TerminalWindow = Window & {
    term?: {
        paste: (text: string) => void;
        scrollToBottom?: () => void;
        attachCustomKeyEventHandler?: (customKeyEventHandler: (event: KeyboardEvent) => boolean) => void;
        buffer?: {
```
I will add `attachCustomKeyEventHandler` there.
Wait, let's verify if `term.customKeyEventHandler` is available, maybe we can wrap the existing one if any.
```typescript
        customKeyEventHandler?: (event: KeyboardEvent) => boolean;
```
If we do:
```typescript
        const existingCustomKeyEventHandler = term.customKeyEventHandler;
        term.attachCustomKeyEventHandler?.((event: KeyboardEvent) => {
            if (event.type === 'keydown' && event.metaKey && (event.key === 'Backspace' || event.key === 'Delete')) {
                term.paste('\x15');
                return false;
            }
            if (existingCustomKeyEventHandler) {
                return existingCustomKeyEventHandler(event);
            }
            return true;
        });
```
This is safe.
